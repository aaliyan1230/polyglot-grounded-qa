run_name: free-cloud-unsloth-qlora
framework: unsloth
objective: grounded-policy-sft

model:
  name: unsloth/Qwen2.5-7B-Instruct-bnb-4bit
  load_in_4bit: true

lora:
  rank: 16
  alpha: 16
  dropout: 0.0
  use_gradient_checkpointing: unsloth
  target_modules:
    - q_proj
    - k_proj
    - v_proj
    - o_proj
    - gate_proj
    - up_proj
    - down_proj

training:
  max_seq_length: 1536
  per_device_train_batch_size: 1
  gradient_accumulation_steps: 16
  learning_rate: 2.0e-4
  warmup_steps: 20
  max_steps: 400
  logging_steps: 10
  save_steps: 50
  optim: adamw_8bit
  seed: 42

data:
  train: data/benchmarks/finetune/formatted/train.text.jsonl
  val: data/benchmarks/finetune/formatted/val.text.jsonl
  test: data/benchmarks/finetune/formatted/test.text.jsonl

runtime:
  preferred: kaggle-or-colab-free
  gpu: t4
  gpu_count: 1
  session_strategy: resumable-checkpoints
  checkpoint_dir: artifacts/runs/finetune_checkpoints

acceptance:
  min_abstain_precision_delta: 0.05
  min_citation_precision_delta: 0.03
  max_answer_regression: 0.02
